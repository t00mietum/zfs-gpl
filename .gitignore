/target
**/*.rs.bk
*.swp
*.orig

# defensive: nothing from the reference/dirty tree may ever land here
/openzfs/
/reference-source/
**/opensolaris/
**/*.cddl

# local Claude working files (memory, local settings); live outside the repo, never commit
/.claude/
